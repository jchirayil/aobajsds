//src/base/dataset-handler.ts

import { DatasetCore } from './dataset-core';

export class DatasetHandler extends DatasetCore {
    constructor(name: string) {
        super(name);
    }
    // Add methods and properties specific to DatasetHandler here
}
